# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

pool:
  vmImage: 'Ubuntu 16.04'

steps:
- script: products-service/cicd-scripts/build.sh
  displayName: 'Build products-service Container'
  inputs:
    dockerRegistry: bricerisingslalom

- script: catalog-service/cicd-scripts/build.sh
  displayName: 'Build products-service Container'
